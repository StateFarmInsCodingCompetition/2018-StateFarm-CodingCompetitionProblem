package com.codingcompetition.statefarm;

/**
 * Represents a strategy used to match the values of the descriptors in points of interest.
 */
public enum MatchingStrategy {
    EXACT_MATCH, STARTS_WITH, ENDS_WITH;
}
